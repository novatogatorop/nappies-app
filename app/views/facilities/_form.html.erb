<%= simple_form_for(@facility) do |f| %>
  <%#= p.error_notification %>
  <%#= p.error_notification message: p.object.errors[:base].to_sentence if p.object.errors[:base].present? %>

  <div class="form-inputs">
    <p><%= f.input :name %></p>
    <p><%= f.input :photo, as: :file %></p>
  </div>

  <div class="form-actions mt-5">
    <%= f.button :submit, :class => 'account-submit' %>
    <%= link_to 'Cancel', places_path, :class => 'no-decor-color' %>
  </div>
<% end %>
