<%#= form_for :search, url: places_path, class: "search-form", method: :get do %>
  <!-- <div class="search-box"> -->
      <%#= text_field_tag :query, params[:query],
        class: "search-input placeholder-search",
        placeholder: "City, place name, type..."%>

      <%#= collection_select :type, :id, Type.all.order('name ASC'), :id, :name,{include_blank: 'Select Type'}, class: "select2 search-select" %>
      <%#= select_tag "facility", options_for_select(['Select Facility',
                'Changing Table',
                'Toy',
                'High Chair',
                'Play Area']), class: "select2 search-select" %>
      <%#= submit_tag "🔎", class: "search-submit"  %>
      <!-- <button type="submit" class="search-submit"><i class="fas fa-search"></i></button> -->
  <!-- </div> -->
<%# end %>

<%# query = params[:search][:query] if !params[:search].nil?%>

<%= simple_form_for :search, url: places_path, method: "GET", html: { class: 'search-form' } do |f| %>
<div class="search-box">
  <%= f.input :query, label: false, placeholder: "City, place name, type...", input_html: { class: "search-input placeholder-search", value: "" } %>
  <%= f.input :type, as: :select, :collection => @types, label: false, placeholder: "City, place name, type...", input_html: { class: "select2 search-select" } %>
  <%= f.input :changing_table, as: :boolean, checked_value: true, unchecked_value: false %>
  <%= f.input :high_chair, as: :boolean, checked_value: true, unchecked_value: false %>
  <%= f.input :toy, as: :boolean, checked_value: true, unchecked_value: false %>
  <%= f.input :play_area, as: :boolean, checked_value: true, unchecked_value: false %>
  <button type="submit" class="search-submit"><i class="fas fa-search"></i></button>
</div>
<% end %>


